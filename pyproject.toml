[tool.poetry]
name = "aws-image-manager"
version = "0.1.0"
description = ""
authors = ["kishore-0514 <kishore@aggregateintelligence.in>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
